# Push only the scripts and the manifest; everything else in rootDir stays local.
# NB: this is an allow-list (ignore **/** then un-ignore) — a NEW .gs file is pushed only
# if it is added here, so every script needs its own `!<file>.gs` line.
**/**
!gmail-collector.gs
!vacancies-backup.gs
!appsscript.json
