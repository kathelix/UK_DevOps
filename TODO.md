# TODO

Improvements deliberately deferred during the 1:1 Make.com → Apps Script port of the Gmail collector (see `apps-script/README.md`). Roughly in priority order.

## Roadmap: GAS collector rollout (agreed 2026-06-07)

Goal: new GAS script runs end-to-end — deployed from CI, fed by time trigger, consumed by Claude from Airtable.

- [x] **M6.1 — Externalize the instructions (field → repo-file stub)** — done (this slice). The claude.ai project field is now a **bootstrap pointer** (`instructions/PROJECT_FIELD_STUB.md`, owner-pasted) → reads `instructions/Claude_project_instructions.md` from the mounted UK_DevOps folder. **Local-file-primary, fail loud if the folder isn't attached, no network/cache/memory fallback** (an absent mount halts the run rather than screening on stale instructions). Instructions body unchanged at `VERSION: 1.2` — loading mechanism only, no screening-behaviour change. Owner-activation checklist (incl. the "verify scheduled runs mount the folder" risk gate) + runbook: `docs/OPERATIONS.md` → "Instructions loading"; decision + rejected alternatives: `docs/TECH_DESIGN.md` §6.
- [x] **M6.2 — Intake cutover (bump to `VERSION: 2.0`)** — done (this slice). Block 1 §1 rewritten (intake = RawEmails `Status=New`, Gmail demoted to a **discrepancy canary only** — an Airtable outage alerts and stops, no Gmail-direct screening fallback per Codex F1/owner 2026-06-16), §9 (mark-as-read → Status flip to `Processed`, with the Gmail mark-read **kept** for the canary/inbox "unread ⟺ not-yet-processed" invariant), §3 reframed to the pre-cleaned `CleanText`, and the cross-source vacancy identity rule shipped as §5a. Parity was confirmed 2026-06-15. The screening run must execute in a session with the UK_DevOps folder attached (M6.1's fail-loud stub halts otherwise). **Make was kept running** as the parallel safety net during M6.2 — decommission was a separate later task (done 2026-06-17, below), not part of M6.2. One-time activation incl. the required pre-cutover backlog migration + rollback: `docs/OPERATIONS.md` → "Intake cutover (M6.2)"; shipped-state design: `docs/TECH_DESIGN.md` §6.
- [x] **M6.3 — §6a live link-resolution pass (Claude-in-Chrome), bump to `VERSION: 2.1`** — done (this slice). Folded into Block 1 §6a as an **interactive-only** verifier over the two final lists (Recommend + Flag): open each role's resolved canonical link in Chrome (`navigate` → `get_page_text`), confirm live + open, re-verify the non-negotiable gates on the rendered page, **upgrade** a Flag that proves genuine (now > 75%) / **drop** a Recommend/Flag exposed as aggregator-fiction or a dead/closed scrape, **auto-skip** closed listings — drilling through aggregator cards to the real LinkedIn/ATS/employer source (outsideir35.org.uk lies about work model / rate-unit / open-status — 2026-06-17). **Recommends first**; scope-disciplined to the two lists, **not** every email link. Cookie-accept owner-pre-authorised for these pages (2026-06-11); the verified live-source URL is stored in the Airtable `Link` field. **VPN is remind-only** (connect Total VPN 2 → UK before the pass; a geo-reject = VPN-not-connected, never a dead listing). §8 gains the dated `<date>_recommend-flag.md` **handoff file** the unattended scheduled run persists so a separate interactive session can run the pass; the scheduled run stays **offline** (§6a steps 1–5) and never does Chrome work. Behaviour-additive (no new gates/weights/bands; §1/§9 untouched); validation is an interactive owner live-run (no unit test guards instructions), rollback is `git revert`. Design: `docs/TECH_DESIGN.md` §6; runbook: `docs/OPERATIONS.md` → "Live link resolution (Chrome pass)". Flagged-only proven 2026-06-11 (5/5 flags resolved: 2 upgraded, 3 exposed); Recommends added 2026-06-14 (Ivan: "Recommended vacancies are obviously even more important").
  - [ ] **Deferred follow-up — VPN automation (stretch).** Drive **Total VPN 2** (macOS app) via computer use: connect to a UK server at pass start, disconnect at end — replacing the remind-only step above. Out of scope for M6.3 (owner decision 2026-06-17).
- [ ] **Footer-freshness analysis — Cowork-side daily scan (idea raised by Ivan 2026-06-14)**, fold into Block 1 as a screening-side step now that intake reads RawEmails (**M6.2 shipped → unblocked**):
  - **What.** During daily screening, after reading the day's cleaned emails from Airtable `RawEmails`, scan their footers and surface any *new* footer (sender not yet in the collector's `FOOTER_MARKERS`) or *changed* footer (text drifted from the known marker). Fuzzy pattern-spotting, so it lives with Claude/Ivan — explicitly not GAS.
  - **Why it's additive, not duplicate.** The GAS-side `truncateAtFooter_` template-change alarm (Issue #14, done; TECH_DESIGN §4) only fires for *mapped* senders whose marker goes missing, and only as a run-failure email — "unmapped senders never alarm." This Cowork pass covers the gap: footers on not-yet-mapped senders, plus marker drift, surfaced inline so Ivan sees it the same day without digging into Airtable/Gmail.
  - **Surface only when detected.** Add an inline alert to the batch report only when a new/changed footer is spotted; stay silent on clean days (mirrors the §8 "don't prompt if every role was a clean accept/reject" pattern).
  - **Output → action.** When flagged, propose a candidate marker string — entity-free, taken from stored `CleanText` byte-form (per the footer-fixture-capture recipe) — so `FOOTER_MARKERS` / the `.gs` can be updated. This is the human-in-the-loop feeder that keeps the collector's marker map current.

## Collector (`apps-script/gmail-collector.gs`)

- [ ] **Fetch via label store instead of search index.** The `q=`-based listing (inherited from Make) reads Gmail's search index, which silently skips unindexed messages — observed 2026-06-07 with securityclearedjobs.com emails: visible in the Gmail UI, invisible to every API query (`from:`, `subject:`, `in:anywhere`). Switch to label-store listing (`getUserLabelByName('job-vacancies')` / `labelIds`-based) to make such orphans structurally impossible.
- [x] **`make-failed` on a repeatedly-transient write / set `make-processing`** — done (the cap) + decided (no `make-processing`). A record whose *own* Airtable write fails **transiently** (`429`/`5xx`/transport) on every run is now capped: a cross-run **Script Properties strike counter** (`wretry:<messageId>`) `make-failed`s it after `MAX_TRANSIENT_WRITE_RETRIES` (default 5, runtime-tunable, bounds `[1,100]`) **consecutive** strikes, so it stops retrying forever and failing the run every run; the counter clears on any successful upsert or on quarantine. Record-specificity is **sticky** (Codex F1 fix, Option 1): the **first** strike needs a same-run healthy sibling, then the counter carries the proof so the record keeps striking even once its siblings are collected and it is alone (else the cap is unreachable). A **fresh** never-struck message in a systemic outage never strikes (mass-quarantine guard, holds for the bulk of the queue); two accepted residuals (struck-then-outage, solo-from-start) are documented in `docs/TECH_DESIGN.md` §2 / `docs/OPERATIONS.md`. `DRY_RUN` does no strike/quarantine/Script-Properties write. **`make-processing` is deliberately NOT set** (as in Make): single-flight + the idempotent write-then-label already give crash-safety, so a mid-flight marker would add persistent state for no benefit (owner decision 2026-06-15). The cross-run counter stays **rejected for the deterministic case** (slower, doesn't unblock siblings — handled within-run by the per-record isolation). See `docs/TECH_DESIGN.md` §2 (repeatedly-transient write cap) and `docs/OPERATIONS.md` (the `wretry:`/`MAX_TRANSIENT_WRITE_RETRIES` runbook).
- [ ] **Drop the stale `make-processing` token from `CONFIG.QUERY`** (minor cleanup, deferred from the transient-write-cap slice). `make-processing` is never set (decided above), so the `-label:job-vacancies/make-processing` clause in `CONFIG.QUERY` is a harmless no-op. A `QUERY` change was out of that slice's scope; remove the token standalone or fold it into the **Make.com leftovers** label-rename sweep below (which already plans to retire the `make-*` label names).
- [x] **Failure alerting — screening-side canary** — done at M6.2. The screening-side half shipped as instructions §1 Path 2 (the discrepancy canary): "0 New RawEmails rows but unread job-vacancies mail in Gmail" raises a collector-failure alert rather than a quiet day, documented in `docs/OPERATIONS.md` → "Canary: missing-email check" as the post-cutover primary check. _(The script half was already done: collector and purge executions end Failed on any upsert/list/delete failure or a persistent Gmail-read (`get`/`list`) failure, so GAS failure emails fire.)_
- [ ] **Second cleaning pass.** The regex strips attributes/comments/images but leaves bare tag skeletons (`<td>`, `<tr>`, `<a href>`) and undecoded entities (`&amp;`, `&pound;`). Add a tag-to-text pass (newlines at block boundaries, entity decode) — meaningful token saving for the screening step. Measured 2026-06-07 (NIJobs single-rec, 47.2KB html → 19.4KB clean): ~3.5KB is invisible-entity preheader padding (`&#847;&zwnj;&shy;` walls) the regex doesn't target; real content is only ~5KB. _The single-child table-wrapper unwrap (#13) already landed as the safe incremental step; tag→text would subsume its saving, at which point the unwrap retires or becomes its pre-stage — see `docs/TECH_DESIGN.md` §4._
- [ ] **Modularize for testability** — split into `config / gmail / parser / airtable / main`; keep cleaning, link-extraction and dedupe as pure functions (no Gmail/Airtable side effects) so they run against `tests/fixtures/` locally. _(Remaining: the full module split — `buildUpsertPayload_` and `isOverRuntimeBudget_` are already extracted as pure, unit-tested helpers.)_

### Reliability

- [x] **Gmail-read retry** (`Messages.get`/`list`) — done. `gmailReadWithRetry_` retries an idempotent Gmail read on *any* throw (`[1s,2s,4s]`, budget-aware, no HTTP-status parsing — Option B deferred); the collect loop's formerly-single per-message `try/catch` is split into two narrow ones, so a *transient* `get` blip leaves the message uncollected (retried next run, **never** `make-failed`) instead of mis-quarantining it (the latent data-loss bug), while a deterministic `processMessage_` parse failure still `make-failed`s. `Messages.list` is wrapped too; a persistent read failure ends the run Failed and folds into the one fail-loud throw alongside upsert + footer signals. See `docs/TECH_DESIGN.md` §2.

## Airtable

- [x] **Import the existing Vacancies table schema into `airtable/schema.json`** — done. Vacancies is now version-controlled (with field ids); `apply-schema.js` matches **by field ID when present**, so a UI rename surfaces as a rename-drift warning instead of a duplicate field; and `airtable/import-schema.js` (GET Meta API → idempotent merge, managed-table allowlist) backfills ids / snapshots drift. Both the diff and merge paths are pure, fixture-tested (`tests/apply-schema.test.js`, `tests/import-schema.test.js`); `tests.yml` now also runs on `airtable/**`. The orphaned M5 `Vacancies_test` sandbox mirror was **retired** from `schema.json` + the managed allowlist in the same PR (owner deletes the now-unmanaged live table manually post-merge — additive CI / the Meta API can't). **Residual:** RawEmails stays id-less in `schema.json` until the owner runs `import-schema.js` once with a token (its rename-safety lands then) — see `docs/KNOWN_ISSUES.md` §3.

## Pipeline integration

- [x] **Switch the Claude screening pipeline (project instructions Block 1 §1) from Gmail search to RawEmails** — done at M6.2 (see the roadmap above): read `Status=New` → screen → flip to `Processed`, with the Gmail connector kept as the **discrepancy canary only** (an Airtable outage alerts and stops — no Gmail-direct screening fallback).
- [x] **Decommission the Make.com scenario** — done 2026-06-17. Make ran as the parallel safety net for the first 2.0 runs (parity confirmed 2026-06-15), then was decommissioned; the GAS collector is now the sole pipeline. (It also freed the 1,000 free ops/month it had been burning.)
- [ ] **Rename the `make-*` state labels (the remaining Make.com leftover)** — `job-vacancies/make-collected|processing|failed` → tool-neutral (preferred, e.g. `job-vacancies/collected|…`) or `gas-*`. Its own slice: a **live Gmail label migration** (re-label already-tagged mail) + the `CONFIG.QUERY` / `QUERY`-token changes (folds in the stale `make-processing` token drop above) + ~90 test refs + a coordinated collector deploy. _(The Make-brand **wording sweep** and the `UK_DevOps_Gmail_Collector.blueprint.json` relic deletion were completed 2026-06-17 — the de-make-repo slice.)_

## Developer experience

- [ ] **Fix Code session renaming**: fix the `scripts/pr-session-name.sh`, currently symlink-ed `~/.claude/hooks/pr-session-name.sh` that should rename a Claude Code sesion to "PR#nn" once session opens a new PR. Debug in the existing session that already created a PR, because it has all the logs from this hook and script.
- [ ] **Revisit Cowork-Code handover approach**: Currently parked in `scripts/slice-passing-parked``
